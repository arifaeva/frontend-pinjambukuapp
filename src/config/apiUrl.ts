export const API_URL =
  process.env.NODE_ENV === "production"
    ? "http://localhost:8000"
    : "http://localhost:8000";
// export const API_URL = process.env.NODE_ENV === "production" ? "domain" : "http://localhost:8000";
